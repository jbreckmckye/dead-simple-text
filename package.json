{
  "name": "dead-simple-text",
  "version": "1.0.0",
  "description": "Sometimes you just need plain text",
  "main": "index.js",
  "scripts": {
    "build": "parcel build src/index.html --experimental-scope-hoisting --public-url /dead-simple-text/",
    "bundle": "zip -r dist.zip ./dist",
    "clean": "rm -rf dist",
    "start": "parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "plain-text",
    "text-editor"
  ],
  "author": "Jimmy Breck-McKye",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "4.9.3",
    "parcel-bundler": "1.10.0",
    "sass": "1.15.1",
    "typescript": "3.0.3"
  },
  "dependencies": {}
}
